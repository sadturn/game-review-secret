<h1>Reviews#edit</h1>
<p>Find me in app/views/reviews/edit.html.erb</p>

<%= form_for @review, url: review_path(@review.id) do |f| %>

    <% if @review.errors.any? %>
   <div id="error_explanation">
     <h2><%= pluralize(@review.errors.count, "error") %> prohibited this post from being saved:</h2>
  
     <ul>
     <% @review.errors.full_messages.each do |msg| %>
       <li><%= msg %></li>
     <% end %>
     </ul>
   </div>
 <% end %>
 
   <%= f.text_field :title %><br />
   <%= f.text_area :body, size: "60x12" %><br />
   <%= f.submit "Create" %>
 <% end %>
